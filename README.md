# PHP CMS Blog

## Files

`admin.php` - Adding, editing and deleting posts.  
`delete.php` - Deletes a post from the form on `edit.php`.  
`edit.php` - Edit posts  
`index.php` - See all posts  
`login.php` - Login to add, edit or delete posts  
`schema.sql` - DB Table creation
